# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build wlp-google-batchs`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t wlp-google-batchs`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system wlp-google-batchs && \
          adduser --system -G wlp-google-batchs wlp-google-batchs

COPY dist/apps/wlp-google-batchs wlp-google-batchs/
RUN chown -R wlp-google-batchs:wlp-google-batchs .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix wlp-google-batchs --omit=dev -f install

CMD [ "node", "wlp-google-batchs" ]
